-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.BiometricVerification (
  id text NOT NULL,
  investmentRequestId text NOT NULL,
  status text NOT NULL DEFAULT 'PENDING'::text,
  modality text NOT NULL DEFAULT 'FACE'::text,
  metadata jsonb,
  verifiedAt timestamp without time zone,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT BiometricVerification_pkey PRIMARY KEY (id),
  CONSTRAINT BiometricVerification_investmentRequestId_fkey FOREIGN KEY (investmentRequestId) REFERENCES public.InvestmentRequest(id)
);
CREATE TABLE public.CreditSimulation (
  id text NOT NULL,
  userId text NOT NULL,
  creditTypeId text NOT NULL,
  amount double precision NOT NULL,
  termMonths integer NOT NULL,
  amortizationSystem text NOT NULL,
  paymentSchedule jsonb NOT NULL,
  totalPayment double precision NOT NULL,
  totalInterest double precision NOT NULL,
  totalCharges double precision NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT CreditSimulation_pkey PRIMARY KEY (id),
  CONSTRAINT CreditSimulation_userId_fkey FOREIGN KEY (userId) REFERENCES public.User(id),
  CONSTRAINT CreditSimulation_creditTypeId_fkey FOREIGN KEY (creditTypeId) REFERENCES public.CreditType(id)
);
CREATE TABLE public.CreditType (
  id text NOT NULL,
  name text NOT NULL,
  minAmount double precision NOT NULL,
  maxAmount double precision NOT NULL,
  annualInterestRate double precision NOT NULL,
  amortizationSystems ARRAY,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  institutionId text NOT NULL,
  CONSTRAINT CreditType_pkey PRIMARY KEY (id),
  CONSTRAINT CreditType_institutionId_fkey FOREIGN KEY (institutionId) REFERENCES public.Institution(id)
);
CREATE TABLE public.IndirectCharge (
  id text NOT NULL,
  name text NOT NULL,
  type USER-DEFINED NOT NULL DEFAULT 'FIXED'::"ChargeType",
  value double precision NOT NULL,
  mandatory boolean NOT NULL DEFAULT false,
  creditTypeId text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT IndirectCharge_pkey PRIMARY KEY (id),
  CONSTRAINT IndirectCharge_creditTypeId_fkey FOREIGN KEY (creditTypeId) REFERENCES public.CreditType(id)
);
CREATE TABLE public.Institution (
  id text NOT NULL,
  name text NOT NULL,
  logoUrl text,
  ruc text,
  contact text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT Institution_pkey PRIMARY KEY (id)
);
CREATE TABLE public.InvestmentDocument (
  id text NOT NULL,
  investmentRequestId text NOT NULL,
  type text NOT NULL,
  fileUrl text NOT NULL,
  uploadedAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT InvestmentDocument_pkey PRIMARY KEY (id),
  CONSTRAINT InvestmentDocument_investmentRequestId_fkey FOREIGN KEY (investmentRequestId) REFERENCES public.InvestmentRequest(id)
);
CREATE TABLE public.InvestmentProduct (
  id text NOT NULL,
  name text NOT NULL,
  purpose text,
  minAmount double precision NOT NULL,
  maxAmount double precision NOT NULL,
  minTermMonths integer NOT NULL,
  maxTermMonths integer NOT NULL,
  annualRate double precision NOT NULL,
  allowOutOfMargin boolean NOT NULL DEFAULT false,
  capitalizationFreq text NOT NULL DEFAULT 'MONTHLY'::text,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT InvestmentProduct_pkey PRIMARY KEY (id)
);
CREATE TABLE public.InvestmentRequest (
  id text NOT NULL,
  userId text NOT NULL,
  productId text NOT NULL,
  amount double precision NOT NULL,
  termMonths integer NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'PENDING'::"RequestStatus",
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT InvestmentRequest_pkey PRIMARY KEY (id),
  CONSTRAINT InvestmentRequest_userId_fkey FOREIGN KEY (userId) REFERENCES public.User(id),
  CONSTRAINT InvestmentRequest_productId_fkey FOREIGN KEY (productId) REFERENCES public.InvestmentProduct(id)
);
CREATE TABLE public.User (
  id text NOT NULL,
  email text NOT NULL,
  passwordHash text NOT NULL,
  role USER-DEFINED NOT NULL DEFAULT 'CLIENT'::"Role",
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updatedAt timestamp without time zone NOT NULL,
  CONSTRAINT User_pkey PRIMARY KEY (id)
);